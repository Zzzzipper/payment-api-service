{
    "name": "payment-api-service",
    "env": {
        "SERVE_HTTP": {
            "description": "Whether to use HTTP to serve, instead of HTTPS",
            "value": "true",
            "required": false
        }
    }
  }