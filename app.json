{
    "name": "payment-api-serivce",
    "env": {
        "SERVE_HTTP": {
            "description": "Whether to use HTTP to serve, instead of HTTPS",
            "value": "true",
            "required": false
        }
    }
  }